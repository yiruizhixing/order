<!--pages/my/history/history.wxml-->
<view class="container">
    <view class="comment-list" hidden="true">
        <view class="a-order" wx:for="{{list}}" wx:key="{{index}}" wx:for-item="item">
            <view class="order-date">
                <view class="date-box">考试时间：{{item.date}}</view>
            </view>
            <view class="goods-info">
                <view class="goods-des">
                  <view>考试名称：{{item.order_number}} </view>
                   <view>工作岗位：{{item.content}}</view>
                </view>
            </view>
        </view>
    </view>
    <view class="comment-list">暂无内容</view>
</view>
